# Variables are read from the process environment, not automatically from this file.
HOST=127.0.0.1
PORT=4173
DATA_DIR=./.data
# Set NODE_ENV=production only behind HTTPS; cookies then require Secure transport.
NODE_ENV=development
# Optional exact external HTTPS origin behind a trusted reverse proxy:
# ALLOWED_ORIGIN=https://studio.example.com
