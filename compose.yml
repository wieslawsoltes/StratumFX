services:
  stratum:
    build: .
    ports:
      - "127.0.0.1:4173:4173"
    environment:
      NODE_ENV: development
      HOST: 0.0.0.0
      PORT: 4173
      DATA_DIR: /data
    volumes:
      - stratum-data:/data
    restart: unless-stopped
volumes:
  stratum-data:
